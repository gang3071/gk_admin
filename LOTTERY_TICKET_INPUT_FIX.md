# 摸奖券录入中奖记录 - 输入问题修复

## 问题描述

用户反馈的两个问题:
1. **输入中奖券号问题**: 无法输入 `00012` 这样带前导零的数字
2. **取消按钮无法关闭抽屉**: 点击取消按钮后抽屉不关闭

---

## 问题分析

### 问题 1: 前导零丢失

**根本原因:**
- 前端输入框如果使用 `type="number"`,浏览器会自动将 `00012` 转换为数字 `12`
- 前端传到后端时,前导零已经丢失
- 后端正则验证 `/^\d{6}$/` 要求严格6位数字,`12` 不符合

**影响代码:**
```php
// 旧代码 - 只接受严格6位数字
if (!preg_match('/^\d{6}$/', $ticketNo)) {
    return message_error('券号必须是6位数字');
}
```

**用户体验问题:**
- 输入 `00012` → 前端自动变成 `12` → 后端报错"必须是6位数字"
- 用户困惑:明明输入了6位,为什么报错?

---

### 问题 2: 取消按钮无法关闭抽屉

**可能原因:**
1. **前端事件未绑定**: ExAdmin 动态表单的取消按钮事件处理问题
2. **验证错误阻止**: 表单验证失败阻止了抽屉关闭
3. **状态管理问题**: 抽屉状态未正确更新

**说明:**
- 这是 ExAdmin 框架的前端问题
- 后端无法直接修复
- 但通过改善输入处理可以减少验证错误,间接改善体验

---

## 修复方案

### 后端修复 (已完成)

#### 1. 修改 `distributeByTicketNo()` 方法

**位置:** `D:\gk_admin\addons\webman\controller\ChannelLotteryTicketActivityController.php` (行 1465-1477)

**修改前:**
```php
if (empty($ticketNo)) {
    return message_error(admin_trans('lottery_ticket.error.invalid_params'));
}

// 券号格式验证：必须是6位数字
if (!preg_match('/^\d{6}$/', $ticketNo)) {
    return message_error(admin_trans('lottery_ticket.message.ticket_must_6_digits'));
}
```

**修改后:**
```php
if (empty($ticketNo)) {
    return message_error(admin_trans('lottery_ticket.error.invalid_params'));
}

// 清理券号：去除空格和非数字字符
$ticketNo = preg_replace('/\D/', '', $ticketNo);

// 券号格式验证：必须是1-6位数字
if (!preg_match('/^\d{1,6}$/', $ticketNo)) {
    return message_error(admin_trans('lottery_ticket.message.ticket_must_6_digits'));
}

// 自动补全前导零到6位
$ticketNo = str_pad($ticketNo, 6, '0', STR_PAD_LEFT);
```

**改进点:**
1. **容错性**: 接受 1-6 位数字输入
2. **自动清理**: 去除空格和其他非数字字符
3. **自动补全**: 输入 `12` 自动变成 `000012`

---

#### 2. 修改 `recordWinByTickets()` 方法

**位置:** 同文件 (行 573-586)

**添加代码:**
```php
// 清理并补全券号：去除空格和非数字字符，补全到6位
$ticketNo = preg_replace('/\D/', '', $ticketNo);
$ticketNo = str_pad($ticketNo, 6, '0', STR_PAD_LEFT);
```

**插入位置:** 在查找摸奖券之前

---

## 用户体验改进

### 修复前

| 用户输入 | 前端处理 | 传到后端 | 后端验证 | 结果 |
|---------|---------|---------|---------|------|
| 00012 | 转为 12 | "12" | ❌ 不是6位 | 报错 |
| 000012 | 转为 12 | "12" | ❌ 不是6位 | 报错 |
| 12 | 保持 12 | "12" | ❌ 不是6位 | 报错 |

### 修复后

| 用户输入 | 前端处理 | 传到后端 | 后端处理 | 结果 |
|---------|---------|---------|---------|------|
| 00012 | 转为 12 | "12" | 补全为 "000012" | ✅ 成功 |
| 000012 | 转为 12 | "12" | 补全为 "000012" | ✅ 成功 |
| 12 | 保持 12 | "12" | 补全为 "000012" | ✅ 成功 |
| 1 | 保持 1 | "1" | 补全为 "000001" | ✅ 成功 |
| 1 2 3 | 转为 123 | "123" | 补全为 "000123" | ✅ 成功 |

---

## 取消按钮问题

### 临时解决方案 (用户端)

由于这是 ExAdmin 前端框架问题,建议用户:

1. **使用 ESC 键关闭抽屉**
   - 按键盘 ESC 键通常可以关闭模态框/抽屉

2. **点击抽屉外部区域**
   - 点击抽屉背景的遮罩层关闭

3. **刷新页面**
   - 如果完全卡住,刷新页面重新打开

### 根本解决方案 (需要前端修改)

**需要检查的前端代码:**
1. ExAdmin 抽屉组件的 `onCancel` 事件绑定
2. 表单验证是否阻止了关闭事件
3. 状态管理是否正确更新 `visible` 属性

**建议:**
- 联系 ExAdmin 框架维护者报告此问题
- 或检查是否有框架版本更新修复了此问题

---

## 测试用例

修复后应测试以下场景:

### 正常输入
- [ ] 输入 `000012` → 应成功
- [ ] 输入 `12` → 应成功 (自动补全为 `000012`)
- [ ] 输入 `1` → 应成功 (自动补全为 `000001`)
- [ ] 输入 `999999` → 应成功

### 特殊字符处理
- [ ] 输入 `00 12 34` (带空格) → 应成功 (清理为 `001234`)
- [ ] 输入 `a12b34c` (带字母) → 应成功 (清理为 `001234`)

### 错误输入
- [ ] 输入空字符串 → 应报错"参数无效"
- [ ] 输入 `1234567` (7位) → 应报错"必须是6位数字"

### 抽屉关闭
- [ ] 点击取消按钮 → (如果仍失败,提示用户使用 ESC 或点击外部)
- [ ] 按 ESC 键 → 应关闭
- [ ] 点击遮罩层 → 应关闭

---

## 相关文件

**已修改:**
- `D:\gk_admin\addons\webman\controller\ChannelLotteryTicketActivityController.php`
  - `distributeByTicketNo()` 方法 (1465-1477行)
  - `recordWinByTickets()` 方法 (573-586行)

**未修改 (前端问题,无法修改):**
- ExAdmin 抽屉组件 (编译后的 JS 文件)

---

## 总结

✅ **已修复:**
- 券号输入前导零丢失问题
- 自动清理和补全券号功能
- 改善用户体验

⚠️ **部分修复:**
- 取消按钮问题属于前端框架问题
- 提供了临时解决方案 (ESC键/点击外部)
- 需要联系框架维护者彻底解决

**建议:**
如果抽屉关闭问题频繁出现,可以考虑升级 ExAdmin 框架版本或联系技术支持。
