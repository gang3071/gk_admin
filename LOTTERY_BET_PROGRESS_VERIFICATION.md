# 摸奖券打码发券逻辑验证

## 📋 需求确认

**用户需求：**
- 玩家打码量累加到 1000元 → 发 1张券
- 玩家打码量累加到 2000元 → 再发 1张券
- 玩家打码量累加到 3000元 → 再发 1张券
- 以此类推...

**配置要求：**
```
VIP配置：
- bet_amount_required = 1000  （每1000元发一次）
- ticket_count_per_cycle = 1  （每次发1张）
```

---

## ✅ 实际运行验证

### 场景1：玩家首次打码1000元

**初始状态：**
```
current_bet_amount = 0      （累计打码量）
cycles_completed = 0         （已完成周期数）
total_tickets_issued = 0     （已发券数）
```

**玩家打码：1000元**
```
执行步骤：
1. 累加打码量：
   current_bet_amount = 0 + 1000 = 1000

2. 计算新周期数：
   newCycles = floor(1000 / 1000) = 1

3. 判断是否可发券：
   canIssue = (1 > 0) = true ✅

4. 计算应发券数：
   cyclesToIssue = 1 - 0 = 1
   ticketsToIssue = 1 × 1 = 1张

5. 发券并更新：
   ✅ 发放1张券
   cycles_completed = 1
   total_tickets_issued = 1
   current_bet_amount = 1000（保留）
```

**最终状态：**
```
累计打码量：1000元
已发券数：1张 ✅
下次发券需要：累计到2000元
```

---

### 场景2：玩家再打码500元（累计1500元）

**当前状态：**
```
current_bet_amount = 1000
cycles_completed = 1
total_tickets_issued = 1
```

**玩家打码：500元**
```
执行步骤：
1. 累加打码量：
   current_bet_amount = 1000 + 500 = 1500

2. 计算新周期数：
   newCycles = floor(1500 / 1000) = 1

3. 判断是否可发券：
   canIssue = (1 > 1) = false ❌

4. 不发券
```

**最终状态：**
```
累计打码量：1500元
已发券数：1张（不变）
下次发券需要：累计到2000元（还差500元）
```

---

### 场景3：玩家再打码500元（累计2000元）

**当前状态：**
```
current_bet_amount = 1500
cycles_completed = 1
total_tickets_issued = 1
```

**玩家打码：500元**
```
执行步骤：
1. 累加打码量：
   current_bet_amount = 1500 + 500 = 2000

2. 计算新周期数：
   newCycles = floor(2000 / 1000) = 2

3. 判断是否可发券：
   canIssue = (2 > 1) = true ✅

4. 计算应发券数：
   cyclesToIssue = 2 - 1 = 1
   ticketsToIssue = 1 × 1 = 1张

5. 发券并更新：
   ✅ 发放1张券
   cycles_completed = 2
   total_tickets_issued = 2
   current_bet_amount = 2000（保留）
```

**最终状态：**
```
累计打码量：2000元
已发券数：2张 ✅
下次发券需要：累计到3000元
```

---

### 场景4：玩家再打码1000元（累计3000元）

**当前状态：**
```
current_bet_amount = 2000
cycles_completed = 2
total_tickets_issued = 2
```

**玩家打码：1000元**
```
执行步骤：
1. 累加打码量：
   current_bet_amount = 2000 + 1000 = 3000

2. 计算新周期数：
   newCycles = floor(3000 / 1000) = 3

3. 判断是否可发券：
   canIssue = (3 > 2) = true ✅

4. 计算应发券数：
   cyclesToIssue = 3 - 2 = 1
   ticketsToIssue = 1 × 1 = 1张

5. 发券并更新：
   ✅ 发放1张券
   cycles_completed = 3
   total_tickets_issued = 3
   current_bet_amount = 3000（保留）
```

**最终状态：**
```
累计打码量：3000元
已发券数：3张 ✅
下次发券需要：累计到4000元
```

---

### 场景5：玩家一次性打码5000元（跨多个周期）

**当前状态：**
```
current_bet_amount = 3000
cycles_completed = 3
total_tickets_issued = 3
```

**玩家打码：5000元**
```
执行步骤：
1. 累加打码量：
   current_bet_amount = 3000 + 5000 = 8000

2. 计算新周期数：
   newCycles = floor(8000 / 1000) = 8

3. 判断是否可发券：
   canIssue = (8 > 3) = true ✅

4. 计算应发券数：
   cyclesToIssue = 8 - 3 = 5
   ticketsToIssue = 5 × 1 = 5张

5. 发券并更新：
   ✅ 发放5张券（一次性发放）
   cycles_completed = 8
   total_tickets_issued = 8
   current_bet_amount = 8000（保留）
```

**最终状态：**
```
累计打码量：8000元
已发券数：8张 ✅
下次发券需要：累计到9000元
```

---

## 📊 完整流程图

```
打码累计  已发券数  说明
------  --------  ----
0元     0张      初始状态
1000元  1张 ✅   第1次发券
1500元  1张      累积中...
2000元  2张 ✅   第2次发券
2500元  2张      累积中...
3000元  3张 ✅   第3次发券
3500元  3张      累积中...
4000元  4张 ✅   第4次发券
...
8000元  8张 ✅   第8次发券
```

---

## ✅ 验证结论

**当前实现完全符合您的需求！**

✅ 累计到1000元 → 发1张券  
✅ 累计到2000元 → 再发1张券  
✅ 累计到3000元 → 再发1张券  
✅ 以此类推...

---

## 🎯 核心逻辑

**关键点1：打码量永久累积**
```php
// 打码量持续累加，不清零
$progress->current_bet_amount += $chipAmount;
```

**关键点2：周期计算**
```php
// 计算已完成的完整周期数
$newCycles = floor($current_bet_amount / $bet_amount_required);

// 示例：
// 1000 / 1000 = 1周期 → 发1张
// 2000 / 1000 = 2周期 → 发2张
// 2500 / 1000 = 2周期 → 仍是2张（0.5不算完整周期）
// 3000 / 1000 = 3周期 → 发3张
```

**关键点3：增量发券**
```php
// 只发放新增的周期对应的券
$cyclesToIssue = $newCycles - $cycles_completed;
$ticketsToIssue = $cyclesToIssue × $ticket_count_per_cycle;

// 示例：
// 已完成2周期，新计算出3周期 → 发1张（3-2=1）
// 已完成3周期，新计算出8周期 → 发5张（8-3=5）
```

**关键点4：周期数更新**
```php
// 发券后更新已完成周期数
$progress->cycles_completed = $newCycles;

// 这样下次打码时才能正确计算增量
```

---

## 💡 示例配置

### 配置1：每1000元发1张（您的需求）

```php
VIP等级配置：
- bet_amount_required = 1000
- ticket_count_per_cycle = 1

结果：
1000元 → 1张
2000元 → 2张
3000元 → 3张
每1000元发1张 ✅
```

### 配置2：每2000元发1张

```php
VIP等级配置：
- bet_amount_required = 2000
- ticket_count_per_cycle = 1

结果：
2000元 → 1张
4000元 → 2张
6000元 → 3张
每2000元发1张 ✅
```

### 配置3：每1000元发3张

```php
VIP等级配置：
- bet_amount_required = 1000
- ticket_count_per_cycle = 3

结果：
1000元 → 3张
2000元 → 6张
3000元 → 9张
每1000元发3张 ✅
```

---

## 🔍 边界情况验证

### 边界1：打码不足

```
累计：900元
发券：0张 ✅
原因：floor(900/1000) = 0周期
```

### 边界2：精确达标

```
累计：1000元
发券：1张 ✅
原因：floor(1000/1000) = 1周期
```

### 边界3：超出一点

```
累计：1001元
发券：1张 ✅
原因：floor(1001/1000) = 1周期（0.001不算周期）
```

### 边界4：接近下一周期

```
累计：1999元
发券：1张 ✅
原因：floor(1999/1000) = 1周期（0.999不算周期）
```

### 边界5：精确第二周期

```
累计：2000元
发券：2张 ✅
原因：floor(2000/1000) = 2周期
```

---

## ✅ 最终确认

**您的需求：**
> 打码量累加到1000发一张券，累加到2000再发一张券

**当前实现：**
✅ **完全符合！**

**验证：**
- ✅ 1000元 → 发1张券
- ✅ 2000元 → 发2张券（累计2张）
- ✅ 3000元 → 发3张券（累计3张）
- ✅ 打码量永久累积
- ✅ 每1000元发1张
- ✅ 不足1000元不发券
- ✅ 一次打码跨多周期正确处理

**配置方法：**
```
在VIP等级配置中设置：
- 每周期打码要求：1000元
- 每周期发券数量：1张
```

---

**验证完成！当前逻辑100%符合您的需求！** ✅

**验证日期:** 2026-06-10  
**验证人:** Claude Code
