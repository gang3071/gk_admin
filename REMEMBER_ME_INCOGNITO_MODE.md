# "记住我（15天免登录）"与无痕模式说明

## ❌ 无痕模式不支持"记住我"功能

### 简短回答

**选择15天免登录后，关闭浏览器，无痕模式打开 → 无法自动登录**

这是**正常的、符合预期的行为**，不是系统问题。

---

## 🔍 为什么无痕模式不能自动登录？

### 1. 无痕模式（隐私模式）的工作原理

无痕模式的设计目的是**保护隐私，不留痕迹**：

```
无痕模式特性：
┌─────────────────────────────────────────────┐
│  ❌ 不读取现有的 Cookie                        │
│  ❌ 不读取现有的 localStorage                  │
│  ❌ 不读取现有的 sessionStorage                │
│  ❌ 不读取浏览历史                              │
│  ❌ 不读取已保存的密码                          │
│  ✅ 关闭窗口后清除所有数据                      │
│  ✅ 完全独立的会话（与普通模式隔离）            │
└─────────────────────────────────────────────┘
```

### 2. "记住我"功能的实现原理

登录系统通常将 Token 存储在以下位置之一：

| 存储位置 | 普通模式 | 无痕模式 | 关闭浏览器后 |
|---------|---------|---------|------------|
| **Cookie（持久化）** | ✅ 可读写 | ❌ 不读取旧数据 | ✅ 保留（普通模式） |
| **localStorage** | ✅ 可读写 | ❌ 不读取旧数据 | ✅ 保留（普通模式） |
| **sessionStorage** | ✅ 可读写 | ❌ 不读取旧数据 | ❌ 清除 |

### 3. ExAdmin Token 存储机制

根据常见的实现方式，ExAdmin 的 Token 可能存储在：

```javascript
// 方式 1: Cookie（最常见）
document.cookie = "admin_token=" + token + "; max-age=" + (15 * 24 * 3600) + "; path=/";

// 方式 2: localStorage
localStorage.setItem('admin_token', token);

// 方式 3: sessionStorage（不支持"记住我"）
sessionStorage.setItem('admin_token', token);
```

**无论哪种方式，无痕模式都不会读取普通模式下保存的数据！**

---

## 📊 实际测试场景对比

### 场景 1: 普通模式（支持"记住我"✅）

```
操作流程：
1. 用户登录，勾选"记住我（15天免登录）"
   ↓
2. Token 保存到 Cookie/localStorage（max-age = 15天）
   ↓
3. 关闭浏览器
   ↓
4. 重新打开浏览器（普通模式）
   ↓
5. 访问后台页面
   ↓
6. ✅ 浏览器发送 Cookie 中的 Token
   ↓
7. ✅ 服务器验证 Token 有效（未过期）
   ↓
8. ✅ 自动登录成功！
```

### 场景 2: 无痕模式（不支持❌）

```
操作流程：
1. 用户登录，勾选"记住我（15天免登录）"
   ↓
2. Token 保存到 Cookie/localStorage
   ↓
3. 关闭浏览器
   ↓
4. 重新打开浏览器（无痕模式）
   ↓
5. 访问后台页面
   ↓
6. ❌ 浏览器不发送 Cookie（无痕模式隔离）
   ❌ JavaScript 无法读取 localStorage（无痕模式隔离）
   ↓
7. ❌ 服务器没有收到 Token
   ↓
8. ❌ 跳转到登录页面
```

### 场景 3: 在无痕模式内登录（会话内有效✅）

```
操作流程：
1. 打开无痕模式窗口
   ↓
2. 访问登录页面，输入账号密码登录
   ↓
3. Token 保存到 Cookie/localStorage（仅在无痕会话内）
   ↓
4. 继续使用（不关闭无痕窗口）
   ↓
5. ✅ 可以正常访问后台页面
   ↓
6. 关闭无痕窗口
   ↓
7. ❌ 所有数据被清除（Cookie、localStorage 全部删除）
   ↓
8. 重新打开无痕窗口 → 需要重新登录
```

---

## 🎯 各种模式下"记住我"功能对比

| 使用场景 | 勾选"记住我" | 关闭浏览器后 | 能否自动登录？ |
|---------|------------|------------|--------------|
| **普通模式** | ✅ 是 | 重新打开普通模式 | ✅ **可以**（15天内） |
| **普通模式** | ❌ 否 | 重新打开普通模式 | ✅ 可以（7天内） |
| **无痕模式** | ✅ 是 | 重新打开无痕模式 | ❌ **不可以** |
| **无痕模式** | ❌ 否 | 重新打开无痕模式 | ❌ 不可以 |
| **无痕模式内** | ✅ 是 | 不关闭无痕窗口 | ✅ 可以 |
| **普通→无痕** | ✅ 是 | 普通模式登录后打开无痕 | ❌ **不可以** |
| **无痕→普通** | ✅ 是 | 无痕模式登录后打开普通 | ❌ 不可以 |

---

## 💡 为什么这样设计？

### 1. 隐私保护

无痕模式的核心目的是**隐私保护**：

- ✅ 在公共电脑上使用（网吧、图书馆）
- ✅ 临时登录敏感账号（银行、邮箱）
- ✅ 防止账号被他人使用
- ✅ 关闭窗口后不留任何痕迹

**如果无痕模式支持"记住我"，就失去了隐私保护的意义！**

### 2. 安全性

```
场景：小明在公共电脑上使用无痕模式登录后台

如果无痕模式支持"记住我"：
  ❌ 小明关闭窗口后，下一个人打开无痕模式 → 自动登录小明的账号 → 严重的安全问题！

实际的设计（无痕模式不读取旧数据）：
  ✅ 小明关闭窗口后，所有数据清除 → 下一个人打开无痕模式 → 需要重新登录 → 安全
```

### 3. 浏览器厂商的一致性

所有主流浏览器（Chrome、Firefox、Safari、Edge）的无痕模式都遵循相同的原则：

```
┌─────────────────────────────────────────┐
│  无痕模式设计原则（所有浏览器一致）      │
├─────────────────────────────────────────┤
│  1. 不读取普通模式的数据                 │
│  2. 不向普通模式写入数据                 │
│  3. 关闭窗口后清除所有无痕数据           │
│  4. 每次打开无痕窗口都是全新会话         │
└─────────────────────────────────────────┘
```

---

## ✅ 正确的使用方式

### 需要长期免登录 → 使用普通模式

```
✅ 推荐使用场景：
  - 自己的电脑/手机
  - 安全的办公环境
  - 信任的设备

操作步骤：
  1. 使用普通模式（非无痕）
  2. 登录时勾选"记住我（15天免登录）"
  3. 关闭浏览器后重新打开（普通模式）
  4. ✅ 自动登录，无需输入密码
```

### 需要隐私保护 → 使用无痕模式

```
✅ 推荐使用场景：
  - 公共电脑（网吧、图书馆、会议室）
  - 临时登录
  - 不想保存登录状态

操作步骤：
  1. 使用无痕模式
  2. 登录后正常使用
  3. 使用完毕后关闭无痕窗口
  4. ✅ 所有数据自动清除，不留痕迹
```

---

## 🔍 验证 Token 存储位置（开发者工具）

### 方法 1: 检查 Cookie

1. 登录后台（勾选"记住我"）
2. 按 F12 打开开发者工具
3. 切换到 **Application（应用）** 标签
4. 左侧选择 **Cookies** → 选择网站域名
5. 查找 Token 相关的 Cookie（可能名为 `admin_token`、`token`、`auth_token` 等）

```
查看 Cookie 信息：
- Name: admin_token
- Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
- Domain: admin.yourdomain.com
- Path: /
- Expires: 2026-06-06 15:58:55  ← 应该是15天后
- Max-Age: 1296000  ← 15天 = 1,296,000 秒
- HttpOnly: (可能为 true)
- Secure: (可能为 true)
```

### 方法 2: 检查 localStorage

1. 登录后台（勾选"记住我"）
2. 按 F12 打开开发者工具
3. 切换到 **Application（应用）** 标签
4. 左侧选择 **Local Storage** → 选择网站域名
5. 查找 Token 相关的键（可能名为 `admin_token`、`token`、`auth_token` 等）

```
localStorage 内容示例：
Key: admin_token
Value: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 方法 3: 在无痕模式中验证隔离性

**测试步骤：**

1. **普通模式下登录并勾选"记住我"**
   ```
   F12 → Application → Cookies
   看到：admin_token = xxx (Expires: 15天后)
   ```

2. **打开无痕模式窗口**
   ```
   F12 → Application → Cookies
   看到：（空，没有任何 Cookie）
   
   F12 → Application → Local Storage
   看到：（空，没有任何数据）
   ```

3. **结论**
   ```
   ✅ 验证了无痕模式与普通模式完全隔离
   ✅ 无痕模式无法读取普通模式的 Token
   ✅ 因此无法自动登录
   ```

---

## 📝 常见问题 FAQ

### Q1: 我勾选了"记住我"，为什么无痕模式还要登录？

**A:** 这是**正常的**。无痕模式设计目的就是不读取旧数据，确保隐私安全。

### Q2: 能否让无痕模式也支持"记住我"？

**A:** **不建议，也无法实现**。这违反了无痕模式的设计原则，会带来严重的安全隐患。

### Q3: 我在办公室电脑上，想要长期免登录怎么办？

**A:** 使用**普通模式**（非无痕），勾选"记住我（15天免登录）"即可。

### Q4: 普通模式会不会不安全？

**A:** 在**自己的设备**上使用普通模式是安全的。如果担心安全性：
- ✅ 设置电脑锁屏密码
- ✅ 使用强密码登录后台
- ✅ 定期退出登录
- ❌ 不要在公共电脑上使用普通模式

### Q5: 如何在多台电脑上同时保持登录？

**A:** 每台电脑都使用**普通模式**登录并勾选"记住我"：
- 电脑 A（普通模式）：勾选"记住我" → 15天免登录 ✅
- 电脑 B（普通模式）：勾选"记住我" → 15天免登录 ✅  
- 手机（普通模式）：勾选"记住我" → 15天免登录 ✅

**注意：** 如果系统设置了"唯一登录"，新登录会踢出上次登录。

### Q6: 我关闭了无痕窗口，Token 会保存到普通模式吗？

**A:** **不会**。无痕模式和普通模式是**完全隔离**的：
- 在无痕模式中保存的 Token → 关闭窗口后删除
- 不会传递到普通模式
- 不会影响普通模式的数据

---

## 🎯 最佳实践建议

### 个人设备（推荐：普通模式 + 记住我）

```
场景：自己的电脑、手机、平板

✅ 推荐配置：
  - 使用普通模式（非无痕）
  - 勾选"记住我（15天免登录）"
  - 设置设备锁屏密码

✅ 优点：
  - 15天内免登录，方便快捷
  - 关闭浏览器后重新打开仍然登录
  - 提高工作效率

⚠️ 注意：
  - 不要在公共场合使用"记住我"
  - 长时间不使用建议手动退出
```

### 公共设备（推荐：无痕模式 + 不勾选记住我）

```
场景：网吧、图书馆、会议室、他人电脑

✅ 推荐配置：
  - 使用无痕模式（Ctrl+Shift+N / Cmd+Shift+N）
  - 不勾选"记住我"
  - 使用完毕后关闭无痕窗口

✅ 优点：
  - 关闭窗口后自动清除所有数据
  - 保护账号安全
  - 保护隐私

⚠️ 注意：
  - 每次都需要重新登录（这是预期的）
  - 不要忘记关闭无痕窗口
```

### 临时测试（推荐：无痕模式）

```
场景：开发测试、多账号切换、临时查看

✅ 推荐配置：
  - 使用无痕模式
  - 可以同时打开多个无痕窗口（每个窗口独立会话）
  - 测试完毕后关闭窗口

✅ 优点：
  - 不影响普通模式的登录状态
  - 可以同时测试多个账号
  - 关闭后自动清理
```

---

## 📊 总结

| 需求 | 推荐方案 | 是否支持"记住我" |
|------|---------|----------------|
| 日常办公（自己设备） | 普通模式 + 勾选"记住我" | ✅ **支持**（15天） |
| 公共场所临时使用 | 无痕模式 | ❌ **不支持** |
| 开发测试多账号 | 无痕模式 | ❌ 不支持 |
| 提高安全性 | 无痕模式 | ❌ 不支持 |
| 长期免登录 | 普通模式 + 勾选"记住我" | ✅ **支持**（15天） |

---

**核心结论：**

✅ **普通模式 + 勾选"记住我"** → 关闭浏览器后重新打开（普通模式）→ **自动登录**（15天内）

❌ **无痕模式（任何情况）** → 关闭浏览器后重新打开（无痕模式）→ **无法自动登录**

这是**浏览器的设计原理**，不是系统问题。无痕模式的目的就是**隐私保护，不留痕迹**。

---

**最后更新：** 2026-05-22  
**适用版本：** 所有浏览器的无痕/隐私模式  
**技术原理：** Cookie/localStorage 隔离机制
