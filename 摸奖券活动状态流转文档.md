# 摸奖券活动状态流转文档

> **文档版本**: v1.0.0  
> **更新时间**: 2026-06-16  
> **适用系统**: YJB Admin - 摸奖券管理系统

---

## 📋 目录

1. [活动状态概览](#活动状态概览)
2. [状态流转规则](#状态流转规则)
3. [自动流转机制](#自动流转机制)
4. [手动操作流程](#手动操作流程)
5. [状态码对照表](#状态码对照表)
6. [开发者指南](#开发者指南)

---

## 活动状态概览

### 状态定义

摸奖券活动共有 **8种状态**，按生命周期顺序如下：

| 状态值 | 常量名 | 中文名称 | 英文名称 | 说明 |
|-------|--------|---------|---------|------|
| 0 | STATUS_NOT_STARTED | 未开始 | Not Started | 活动已创建，预热期未到 |
| 4 | STATUS_PREHEATING | 预热期 | Preheating | 活动宣传预热中 |
| 5 | STATUS_BETTING | 打码中 | Betting | 玩家正在打码获取摸奖券 |
| 1 | STATUS_ONGOING | 进行中 | Ongoing | **已废弃，兼容旧数据** |
| 6 | STATUS_DRAWING | 开奖中 | Drawing | 正在进行摇球开奖 |
| 7 | STATUS_DRAWN | 已开奖待发放 | Drawn | 开奖完成，等待店家发放奖励 |
| 2 | STATUS_ENDED | 已结束 | Ended | 活动结束 |
| 3 | STATUS_CLOSED | 已关闭 | Closed | 活动手动关闭（异常终止） |

### 活动时间节点

活动涉及以下关键时间字段（数据表：`lottery_ticket_activity`）：

| 字段名 | 类型 | 说明 | 示例 |
|-------|------|------|------|
| `preheat_start_time` | datetime | 预热期开始时间（可选） | 2026-01-15 00:00:00 |
| `start_time` | datetime | 活动开始时间（必填） | 2026-01-20 00:00:00 |
| `draw_time` | datetime | 开奖时间（可选） | 2026-02-10 20:00:00 |
| `end_time` | datetime | 活动结束时间（必填） | 2026-02-10 23:59:59 |
| `draw_completed_at` | datetime | 实际开奖完成时间（系统填充） | 2026-02-10 20:15:32 |

**时间顺序约束**：

```
preheat_start_time < start_time < draw_time < end_time
```

---

## 状态流转规则

### 标准流转路径

```
┌─────────────┐
│  未开始(0)   │ ← 创建活动时的初始状态
└──────┬──────┘
       │ preheat_start_time 到达
       ↓
┌─────────────┐
│  预热期(4)   │ ← 活动宣传预热，玩家可查看活动
└──────┬──────┘
       │ start_time 到达
       ↓
┌─────────────┐
│  打码中(5)   │ ← 玩家打码获得摸奖券
└──────┬──────┘
       │ draw_time 到达
       ↓
┌─────────────┐
│  开奖中(6)   │ ← 管理员手动摇球开奖
└──────┬──────┘
       │ 摇球完成
       ↓
┌─────────────┐
│已开奖待发放(7)│ ← 店家线下发放奖励
└──────┬──────┘
       │ end_time 到达
       ↓
┌─────────────┐
│  已结束(2)   │ ← 活动生命周期结束
└─────────────┘
```

### 简化流转路径（无预热期）

```
未开始(0) → 打码中(5) → 开奖中(6) → 已开奖待发放(7) → 已结束(2)
```

### 异常流转路径

```
任意状态 → 已关闭(3)  ← 管理员手动关闭（如活动违规、取消等）
```

---

## 自动流转机制

### 定时任务

**任务类**: `process\LotteryActivityStatusTransitionTask`  
**执行频率**: 每分钟一次（秒级: `43 * * * * *`）  
**功能**: 自动检查活动时间节点并流转状态

### 流转逻辑

系统按以下优先级检查并流转状态：

#### 1️⃣ 活动结束检查（最高优先级）

```php
if (当前时间 >= end_time) {
    status = STATUS_ENDED; // 已结束
}
```

#### 2️⃣ 开奖时间检查

```php
if (当前时间 >= draw_time && draw_time 不为空) {
    status = STATUS_DRAWING; // 开奖中
}
```

#### 3️⃣ 活动开始检查

```php
if (当前时间 >= start_time) {
    status = STATUS_BETTING; // 打码中
}
```

#### 4️⃣ 预热期检查

```php
if (当前时间 >= preheat_start_time && preheat_start_time 不为空) {
    status = STATUS_PREHEATING; // 预热期
}
```

#### 5️⃣ 默认状态

```php
else {
    status = STATUS_NOT_STARTED; // 未开始
}
```

### 状态流转触发动作

当状态发生变化时，系统自动执行以下操作：

| 新状态 | 触发动作 | 说明 |
|-------|---------|------|
| 预热期(4) | `onPreheatingStart()` | 发送预热期开始推送通知 |
| 打码中(5) | `onBettingStart()` | 发送活动开始推送通知 |
| 开奖中(6) | `onDrawingStart()` | 停止发券、发送开奖通知 |
| 已结束(2) | `onActivityEnd()` | 清理打码进度、发送结束通知 |

---

## 手动操作流程

### 摇球开奖操作

**功能说明**:
摇球开奖是摸奖券系统的核心环节，由渠道管理员手动触发。系统会根据已发放的摸奖券数量，自动计算摇球范围，随机摇出6个球的号码，并匹配中奖券号。

**操作入口**: 渠道后台 → 摸奖券管理 → 活动列表 → 摇球开奖按钮

**权限要求**:
- ✅ 必须是渠道管理员（Channel Admin）
- ✅ 需要有活动管理页面访问权限（`ChannelLotteryTicketActivityController\index`）
- ⚠️ **注意**: `performBallDraw`、`getBallRanges`、`getBallResult` 这3个方法**没有独立权限控制**，只要能访问活动页面就能调用

**前置条件**:
- ✅ 活动状态必须为 `开奖中(6)`
- ✅ 活动已发放至少1张摸奖券（`current_ticket_no > 0`）
- ✅ 活动 `ball_result` 字段为空（未开奖）

**涉及接口**:

| 接口方法 | 说明 | HTTP方法 | 需要独立权限 |
|---------|------|----------|-------------|
| `performBallDraw()` | 执行摇球开奖 | POST | ❌ 否（无 @auth 注解） |
| `getBallRanges()` | 获取摇球范围（前端展示用） | GET | ❌ 否 |
| `getBallResult()` | 获取摇球结果 | GET | ❌ 否 |
| `batchDistributeActivity()` | 批量发放该活动所有奖励 | POST | ✅ **是**（有 @auth true） |

**权限说明**:
- 前3个方法是 Vue 前端页面的辅助 API，已通过页面访问权限（`index`）控制
- 只有 `batchDistributeActivity` 需要配置独立的权限节点

**操作流程**:

```
1. 点击"摇球开奖"按钮
   ↓
2. 系统获取分布式锁（防止并发开奖）
   ↓
3. 校验活动状态和数据完整性
   ↓
4. 摇出6个球的号码（根据最大券号范围）
   ↓
5. 匹配中奖券号和奖品等级
   ↓
6. 创建中奖记录（status=PENDING 待发放）
   ↓
7. 更新活动状态为 `已开奖待发放(7)`
   ↓
8. 保存摇球结果到 `ball_result` 字段
   ↓
9. 返回开奖结果（中奖券号、奖品等级等）
```

**开奖后数据变化**:

- `activity.status`: `6` (开奖中) → `7` (已开奖待发放)
- `activity.ball_result`: `null` → `[1,2,3,4,5,6]` (JSON格式)
- `activity.draw_completed_at`: `null` → `2026-02-10 20:15:32`
- 创建多条 `lottery_ticket_record` 记录（`status=0` 待发放）

**摇球规则说明**:

摇球的号码范围由已发放券数决定：

```
示例1: 已发15张券（券号 000000~000014）
- 最大券号: 000014
- 1号球(个位): 0~4
- 2号球(十位): 0~1  
- 3-6号球(百千万十万位): 都是0

示例2: 已发128张券（券号 000000~000127）
- 最大券号: 000127
- 1号球(个位): 0~7
- 2号球(十位): 0~2
- 3号球(百位): 0~1
- 4-6号球: 都是0
```

**防并发机制**:

系统使用Redis分布式锁防止重复开奖：

```php
$lockKey = "lottery_draw_lock:{$activityId}";
$lock = Cache::lock($lockKey, 10); // 10秒超时

if (!$lock->get()) {
    return ['success' => false, 'message' => '开奖正在进行中，请勿重复操作'];
}
```

**中奖通知推送**:

开奖成功后，系统会自动向所有中奖玩家推送通知：

```php
// 代码位置: ChannelLotteryTicketActivityController::performBallDraw()
foreach ($result['data']['winning_tickets'] as $winData) {
    LotteryTicketPushService::pushWinNotification($record);
}
```

### 店家发放奖励操作

**操作入口**: 店家后台 → 摸奖券管理 → 中奖记录 → 待发放列表 → 发放按钮

**业务场景**:
- 中奖玩家到线下店家核对券号
- 店家确认身份后，现金发放奖励
- 店家在后台点击"确认发放"

**操作流程**:

```
1. 店家查看待发放中奖记录列表
   ↓
2. 核对玩家券号和身份
   ↓
3. 现场发放现金奖励
   ↓
4. 点击"确认发放"按钮
   ↓
5. 系统更新中奖记录状态为 `已发放(1)`
   ↓
6. 记录发放时间 `claimed_at`
   ↓
7. 记录发放操作员 `handler_admin_id`
```

**发放后数据变化**:

- `lottery_ticket_record.status`: `0` (待发放) → `1` (已发放)
- `lottery_ticket_record.claimed_at`: `null` → `2026-02-11 10:30:00`
- `lottery_ticket_record.handler_admin_id`: `null` → `店家管理员ID`

---

## 状态码对照表

### 完整状态码映射

```php
// Model: addons\webman\model\LotteryTicketActivity

const STATUS_NOT_STARTED = 0; // 未开始
const STATUS_ONGOING = 1;     // 进行中 ⚠️ 已废弃
const STATUS_ENDED = 2;       // 已结束
const STATUS_CLOSED = 3;      // 已关闭
const STATUS_PREHEATING = 4;  // 预热期
const STATUS_BETTING = 5;     // 打码中
const STATUS_DRAWING = 6;     // 开奖中
const STATUS_DRAWN = 7;       // 已开奖待发放
```

### 前端显示建议

| 状态值 | 标签文本 | 标签颜色 | 图标 |
|-------|---------|---------|------|
| 0 | 未开始 | default (灰色) | ⏰ |
| 4 | 预热中 | warning (橙色) | 🔥 |
| 5 | 打码中 | processing (蓝色) | 🎮 |
| 6 | 开奖中 | error (红色) | 🎰 |
| 7 | 已开奖待发放 | success (绿色) | ✅ |
| 2 | 已结束 | default (灰色) | 🏁 |
| 3 | 已关闭 | error (红色) | ❌ |

### 前端交互逻辑

```javascript
// 根据状态显示不同操作按钮
switch(activity.status) {
  case 0: // 未开始
  case 4: // 预热期
  case 5: // 打码中
    // 显示倒计时
    showCountdown(activity.start_time);
    break;
    
  case 6: // 开奖中
    // 显示"摇球开奖"按钮（仅渠道管理员可见）
    if (isChannelAdmin) {
      showDrawButton();
    }
    // 显示直播地址（如果有）
    if (activity.live_url) {
      showLiveStream(activity.live_url);
    }
    break;
    
  case 7: // 已开奖待发放
    // 显示开奖结果
    showBallResult(activity.ball_result);
    // 店家显示"待发放列表"链接
    if (isStoreAdmin) {
      showPendingRecordsLink();
    }
    break;
    
  case 2: // 已结束
    // 显示活动总结
    showActivitySummary(activity);
    break;
    
  case 3: // 已关闭
    // 显示关闭原因
    showCloseReason(activity.close_reason);
    break;
}
```

---

## 摇球开奖操作手册

### 开奖前准备

#### 1. 检查活动状态

```bash
# 在数据库中查询活动状态
SELECT id, name, status, current_ticket_no, ball_result, start_time, end_time
FROM lottery_ticket_activity
WHERE id = {activity_id};
```

**检查项**:
- ✅ `status = 6` (开奖中)
- ✅ `current_ticket_no > 0` (已发放券)
- ✅ `ball_result IS NULL` (未开奖)

#### 2. 查看已发放券数

```sql
-- 查询实际发券数
SELECT COUNT(*) as total_tickets, MAX(ticket_no) as max_ticket_no
FROM lottery_ticket
WHERE activity_id = {activity_id};
```

**数据一致性检查**:
```sql
-- 检查 current_ticket_no 与实际券数是否一致
SELECT 
    a.current_ticket_no as activity_ticket_count,
    COUNT(t.id) as actual_ticket_count,
    ABS(a.current_ticket_no - COUNT(t.id)) as difference
FROM lottery_ticket_activity a
LEFT JOIN lottery_ticket t ON t.activity_id = a.id
WHERE a.id = {activity_id};
```

⚠️ **如果 `difference > 0`，说明数据不一致，需要先修复！**

#### 3. 查看奖品配置

```sql
-- 检查奖品等级配置
SELECT level_rank, level_name, prize_amount, prize_count, won_count
FROM lottery_ticket_prize_level
WHERE activity_id = {activity_id}
ORDER BY level_rank;
```

**确认项**:
- ✅ 至少有1个奖品等级
- ✅ `prize_count > 0` (有奖品库存)
- ✅ `won_count < prize_count` (还有剩余奖品)

---

### 开奖操作步骤

#### 步骤1: 进入渠道后台

1. 登录渠道管理员账号
2. 导航到：**摸奖券管理** → **活动列表**
3. 找到状态为 `开奖中` 的活动

#### 步骤2: 点击"摇球开奖"按钮

前端会显示：
- 当前已发放券数
- 摇球范围（每个球的号码范围）
- 奖品等级列表

#### 步骤3: 确认开奖

点击确认后，系统执行：

```
1. 获取分布式锁（防止并发）
   ↓
2. 检查活动状态（必须为开奖中）
   ↓
3. 检查是否已开奖（ball_result是否为空）
   ↓
4. 验证券数据完整性
   ↓
5. 摇出6个球（基于最大券号范围）
   ↓
6. 匹配中奖券号和奖品等级
   ↓
7. 创建中奖记录（status=待发放）
   ↓
8. 更新活动状态为"已开奖待发放"
   ↓
9. 推送中奖通知给玩家
   ↓
10. 释放分布式锁
```

#### 步骤4: 查看开奖结果

开奖成功后，前端会显示：
- 摇球结果（6个球的号码）
- 中奖券号列表
- 中奖玩家信息
- 奖品等级和金额

---

### 开奖后操作

#### 查看中奖记录

```sql
SELECT 
    r.id,
    r.ticket_no,
    r.prize_amount,
    r.status,
    p.name as player_name,
    p.phone as player_phone
FROM lottery_ticket_record r
LEFT JOIN player p ON p.id = r.player_id
WHERE r.activity_id = {activity_id}
ORDER BY r.prize_amount DESC;
```

#### 统计中奖情况

```sql
-- 按奖品等级统计
SELECT 
    pl.level_name,
    COUNT(r.id) as win_count,
    SUM(r.prize_amount) as total_prize_amount
FROM lottery_ticket_record r
LEFT JOIN lottery_ticket_prize_level pl ON pl.level_rank = r.prize_level AND pl.activity_id = r.activity_id
WHERE r.activity_id = {activity_id}
GROUP BY pl.level_name;
```

#### 导出中奖名单

```sql
-- 完整中奖名单（用于线下核对）
SELECT 
    r.ticket_no as '券号',
    p.name as '玩家姓名',
    p.phone as '手机号',
    pl.level_name as '奖品等级',
    r.prize_amount as '奖金金额',
    CASE r.status 
        WHEN 0 THEN '待发放'
        WHEN 1 THEN '已发放'
        ELSE '其他'
    END as '发放状态',
    r.claimed_at as '发放时间'
FROM lottery_ticket_record r
LEFT JOIN player p ON p.id = r.player_id
LEFT JOIN lottery_ticket_prize_level pl ON pl.level_rank = r.prize_level AND pl.activity_id = r.activity_id
WHERE r.activity_id = {activity_id}
ORDER BY r.prize_amount DESC;
```

---

### 批量发放奖励

**使用场景**: 渠道管理员可以批量发放该活动所有待发放的奖励（适用于线下已核对完毕的情况）

**API接口**: `ChannelLotteryTicketActivityController::batchDistributeActivity()`

**操作步骤**:
1. 确认所有中奖玩家已线下核对身份
2. 确认所有奖金已线下发放
3. 点击"批量发放奖励"按钮
4. 系统将所有 `status=0` 的记录改为 `status=1`

**SQL等效操作**:
```sql
UPDATE lottery_ticket_record
SET 
    status = 1,  -- 已发放
    claimed_at = NOW(),
    handler_admin_id = {admin_id}
WHERE activity_id = {activity_id}
  AND status = 0;  -- 仅更新待发放记录
```

---

### 故障排查

#### 问题1: 点击"摇球开奖"按钮无反应

**可能原因**:
- ❌ 权限不足（缺少 `performBallDraw` 权限）
- ❌ 活动状态不正确（不是"开奖中"）
- ❌ 网络请求失败

**排查步骤**:
```bash
# 1. 检查权限
SELECT * FROM admin_role_permission
WHERE node_id = 'addons\\webman\\controller\\ChannelLotteryTicketActivityController\\performBallDraw';

# 2. 检查活动状态
SELECT id, name, status FROM lottery_ticket_activity WHERE id = {activity_id};

# 3. 查看错误日志
tail -f runtime/logs/webman.log | grep "performBallDraw"
```

---

#### 问题2: 提示"活动未发放任何摸奖券"

**原因**: `current_ticket_no = 0` 或数据不一致

**解决方案**:
```sql
-- 检查实际券数
SELECT COUNT(*) FROM lottery_ticket WHERE activity_id = {activity_id};

-- 如果有券但 current_ticket_no=0，手动修复
UPDATE lottery_ticket_activity
SET current_ticket_no = (
    SELECT COUNT(*) FROM lottery_ticket WHERE activity_id = {activity_id}
)
WHERE id = {activity_id};
```

---

#### 问题3: 提示"开奖正在进行中，请勿重复操作"

**原因**: Redis分布式锁未释放（可能前一次请求异常中断）

**解决方案**:
```bash
# 清除Redis锁（锁会在10秒后自动过期）
redis-cli
> DEL lottery_draw_lock:{activity_id}
```

---

#### 问题4: 摇球成功但未创建中奖记录

**可能原因**:
- 数据库事务回滚
- 奖品等级配置错误

**排查步骤**:
```sql
-- 1. 检查 ball_result 是否已保存
SELECT ball_result FROM lottery_ticket_activity WHERE id = {activity_id};

-- 2. 检查中奖记录
SELECT COUNT(*) FROM lottery_ticket_record WHERE activity_id = {activity_id};

-- 3. 检查奖品等级
SELECT * FROM lottery_ticket_prize_level WHERE activity_id = {activity_id};
```

**手动匹配中奖券号** (如需重建中奖记录):
```sql
-- 根据摇球结果手动创建中奖记录
-- 摇球结果示例: [0,0,0,0,1,4] → 券号 000014
INSERT INTO lottery_ticket_record (
    activity_id, player_id, department_id, ticket_id, ticket_no, 
    prize_type, prize_name, prize_amount, status
)
SELECT 
    {activity_id},
    t.player_id,
    t.department_id,
    t.id,
    t.ticket_no,
    1,  -- 现金
    pl.level_name,
    pl.prize_amount,
    0   -- 待发放
FROM lottery_ticket t
JOIN lottery_ticket_prize_level pl ON pl.activity_id = {activity_id} AND pl.level_rank = 1
WHERE t.activity_id = {activity_id}
  AND t.ticket_no = '000014';  -- 替换为实际中奖券号
```

---

## 开发者指南

### 获取活动当前状态

```php
use addons\webman\model\LotteryTicketActivity;

$activity = LotteryTicketActivity::find($activityId);

// 获取状态值
$status = $activity->status;

// 获取状态文本
$statusText = LotteryTicketActivity::getStatusText($status);

// 判断是否可以开奖
if ($status === LotteryTicketActivity::STATUS_DRAWING && empty($activity->ball_result)) {
    // 可以摇球开奖
}
```

### 手动更新活动状态

```php
use addons\webman\model\LotteryTicketActivity;

$activity = LotteryTicketActivity::find($activityId);

// 记录状态变更历史
$activity->recordStatusChange(
    LotteryTicketActivity::STATUS_DRAWING, 
    '管理员手动设置为开奖中'
);

$activity->save();
```

### 监听状态变化

在 `LotteryTicketActivity` 模型中使用 Eloquent 事件：

```php
protected static function boot()
{
    parent::boot();
    
    static::updating(function ($model) {
        if ($model->isDirty('status')) {
            $oldStatus = $model->getOriginal('status');
            $newStatus = $model->status;
            
            // 触发自定义状态变化逻辑
            event(new ActivityStatusChanged($model, $oldStatus, $newStatus));
        }
    });
}
```

### 批量查询特定状态活动

```php
// 查询所有进行中的活动（打码中 + 开奖中）
$activeActivities = LotteryTicketActivity::whereIn('status', [
    LotteryTicketActivity::STATUS_BETTING,
    LotteryTicketActivity::STATUS_DRAWING,
])->get();

// 查询未结束的活动
$ongoingActivities = LotteryTicketActivity::whereNotIn('status', [
    LotteryTicketActivity::STATUS_ENDED,
    LotteryTicketActivity::STATUS_CLOSED,
])->get();
```

### 检查活动是否可以摇球

```php
/**
 * 检查活动是否可以摇球开奖
 * 
 * @param int $activityId
 * @return array
 */
public static function canPerformDraw(int $activityId): array
{
    $activity = LotteryTicketActivity::find($activityId);
    
    if (!$activity) {
        return ['can_draw' => false, 'reason' => '活动不存在'];
    }
    
    // 检查状态
    if ($activity->status !== LotteryTicketActivity::STATUS_DRAWING) {
        return [
            'can_draw' => false, 
            'reason' => '活动状态不正确，当前状态：' . LotteryTicketActivity::getStatusText($activity->status)
        ];
    }
    
    // 检查是否已开奖
    if (!empty($activity->ball_result)) {
        return ['can_draw' => false, 'reason' => '活动已完成开奖'];
    }
    
    // 检查是否有发放的券
    if ($activity->current_ticket_no <= 0) {
        return ['can_draw' => false, 'reason' => '活动未发放任何摸奖券'];
    }
    
    return ['can_draw' => true, 'reason' => ''];
}
```

### 状态历史记录

活动支持状态变更历史记录功能（需要模型方法支持）：

```php
// 记录状态变更
$activity->recordStatusChange($newStatus, '变更原因');

// 查询状态历史
$history = $activity->statusHistory()->orderBy('created_at', 'desc')->get();

// 示例数据结构
[
    {
        'activity_id': 1,
        'old_status': 5,
        'new_status': 6,
        'reason': '系统自动流转',
        'operator_id': null,
        'created_at': '2026-02-10 20:00:00'
    }
]
```

---

## 常见问题

### Q1: 活动状态为何会从"打码中"直接跳到"已结束"？

**A**: 如果活动配置时 **未设置 `draw_time`（开奖时间）**，则系统会在 `end_time` 到达时直接将状态从 `打码中(5)` 流转为 `已结束(2)`，跳过开奖环节。

**解决方案**: 
- 创建活动时务必设置 `draw_time` 字段
- `draw_time` 应在 `start_time` 和 `end_time` 之间

---

### Q2: 开奖后发现中奖记录错误，能否重新开奖？

**A**: **不能**。一旦摇球完成，`ball_result` 字段已保存，系统的防重复开奖机制会拒绝再次开奖。

**防重复开奖检查**:
```php
// 代码位置: LotteryBallDrawService::performDraw()
if (!empty($activity->ball_result)) {
    return ['success' => false, 'message' => '活动已完成开奖，不能重复开奖'];
}
```

**解决方案**:
1. **方案1（推荐）**: 直接在数据库修改 `lottery_ticket_record` 表的中奖记录
   - 修改 `prize_amount`（奖金金额）
   - 修改 `prize_level`（奖品等级）
   - 修改 `ticket_no`（中奖券号）

2. **方案2（危险操作，需技术人员）**: 
   - 手动将 `activity.ball_result` 设为 `null`
   - 将 `activity.status` 改回 `6` (开奖中)
   - 删除已创建的中奖记录（`DELETE FROM lottery_ticket_record WHERE activity_id = ?`）
   - 重新点击"摇球开奖"
   - ⚠️ **注意**: 此操作会丢失之前的开奖记录！

---

### Q3: 活动结束后，还能补发奖励吗？

**A**: **可以**。即使活动状态为 `已结束(2)`，店家仍然可以在后台查看中奖记录并发放奖励。

**操作路径**:
- 店家后台 → 摸奖券管理 → 中奖记录 → 筛选"待发放" → 点击"发放"

---

### Q4: 如何手动关闭一个活动？

**A**: 在渠道后台的活动编辑页面，将状态手动设置为 `已关闭(3)`。

**注意事项**:
- 关闭后的活动无法再次开启
- 已发放的摸奖券仍然有效
- 未发放的奖励仍可手动发放

---

### Q5: 预热期和打码中有什么区别？

**A**: 

| 阶段 | 预热期 | 打码中 |
|-----|-------|-------|
| 玩家可见性 | ✅ 可查看活动详情 | ✅ 可查看活动详情 |
| 打码获券 | ❌ 不可以 | ✅ 可以 |
| 充值赠券 | ❌ 不可以 | ✅ 可以 |
| 使用场景 | 活动宣传预热 | 正式参与活动 |

---

## 附录

### 数据库表结构参考

#### `lottery_ticket_activity` 表

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | int | 主键 |
| name | varchar(100) | 活动名称 |
| status | tinyint | 活动状态（0-7） |
| preheat_start_time | datetime | 预热期开始时间 |
| start_time | datetime | 活动开始时间 |
| draw_time | datetime | 开奖时间 |
| end_time | datetime | 活动结束时间 |
| draw_completed_at | datetime | 实际开奖完成时间 |
| ball_result | json | 摇球结果 `[1,2,3,4,5,6]` |
| current_ticket_no | int | 当前已发券数 |

#### `lottery_ticket_record` 表

| 字段名 | 类型 | 说明 |
|-------|------|------|
| id | int | 主键 |
| activity_id | int | 活动ID |
| player_id | int | 中奖玩家ID |
| ticket_id | int | 中奖券ID |
| ticket_no | varchar(6) | 券号 |
| prize_amount | decimal(10,2) | 奖金金额 |
| status | tinyint | 发放状态（0=待发放,1=已发放） |
| claimed_at | datetime | 发放时间 |
| handler_admin_id | int | 发放操作员ID |

---

### 相关文件路径

#### 模型类
- `D:\gk_admin\addons\webman\model\LotteryTicketActivity.php`
- `D:\gk_admin\addons\webman\model\LotteryTicketRecord.php`

#### 定时任务
- `D:\gk_admin\process\LotteryActivityStatusTransitionTask.php`

#### 服务类
- `D:\gk_admin\addons\webman\service\LotteryBallDrawService.php`
- `D:\gk_admin\addons\webman\service\LotteryTicketPushService.php`
- `D:\gk_admin\addons\webman\service\LotteryTicketBetProgressService.php`

#### 控制器
- `D:\gk_admin\addons\webman\controller\ChannelLotteryTicketActivityController.php`
- `D:\gk_admin\addons\webman\controller\AgentLotteryTicketActivityController.php`
- `D:\gk_admin\addons\webman\controller\StoreLotteryTicketActivityController.php`

---

**© 2026 YJB Admin System. All Rights Reserved.**
