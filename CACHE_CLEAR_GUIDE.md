# Vue 页面显示"未知状态"问题解决指南

## 问题现象
摸奖券活动页面显示"未知狀態"，应该显示"待開獎"或"開獎中"。

## 原因分析
代码已经完整更新（包括控制器翻译传递、翻译文件、Vue 组件），问题是**浏览器缓存了旧的 JavaScript 文件**。

## 解决方案

### 方法1：强制刷新（推荐）⭐

**Windows/Linux:**
- 按 `Ctrl + F5`
- 或 `Ctrl + Shift + R`

**Mac:**
- 按 `Cmd + Shift + R`

---

### 方法2：清空缓存并硬性重新加载

1. 打开浏览器开发者工具（F12）
2. **右键点击**浏览器刷新按钮（地址栏左边的圆圈图标）
3. 选择"**清空缓存并硬性重新加载**"

![清空缓存示意图]
```
┌──────────────────────────┐
│  ⟲  刷新                 │
│  ⟳  硬性重新加载         │
│  ✓  清空缓存并硬性重新加载 │ ← 选这个
└──────────────────────────┘
```

---

### 方法3：手动清除浏览器缓存

**Chrome/Edge:**
1. 按 `Ctrl + Shift + Delete`
2. 选择"缓存的图片和文件"
3. 时间范围选择"全部"
4. 点击"清除数据"

**Firefox:**
1. 按 `Ctrl + Shift + Delete`
2. 勾选"缓存"
3. 点击"立即清除"

---

### 方法4：无痕模式测试

打开无痕窗口（`Ctrl + Shift + N`），访问管理后台，验证是否正常显示。

如果无痕模式正常，证明就是缓存问题，请按方法1-3清除缓存。

---

## 验证是否修复

清除缓存后，刷新页面，检查：

### 1. 状态显示正确 ✅

**待开奖状态：**
- 显示文本：`待開獎`（繁体）或 `待开奖`（简体）
- 标签颜色：橙色
- 卡片边框：橙色粗边框
- 卡片背景：淡橙色渐变

**开奖中状态：**
- 显示文本：`開獎中`（繁体）或 `开奖中`（简体）
- 标签颜色：紫色
- 卡片边框：紫色粗边框
- 卡片背景：淡紫色渐变

### 2. 筛选器选项完整 ✅

状态筛选下拉框应该包含：
```
全部狀態
未開始
進行中
待開獎  ← 新增
開獎中  ← 新增
已結束
已關閉
```

### 3. 操作按钮正确 ✅

**待开奖状态显示的按钮：**
- ✅ "開始開獎"（主要操作）
- ✅ "錄入中獎"
- ✅ "查看詳情"
- ✅ "編輯直播地址"

---

## 开发者调试方法

### 检查网络请求

1. 打开开发者工具（F12）
2. 切换到 **Network（网络）** 标签
3. 刷新页面
4. 找到请求 `getActivities` 或 `index`
5. 查看响应数据 `Response`

**正确的响应应该包含：**
```json
{
  "trans": {
    "pendingDraw": "待開獎",
    "drawing": "開獎中",
    "statusUnknown": "未知狀態"
  }
}
```

### 检查 Console 错误

开发者工具 → Console 标签，查看是否有 JavaScript 错误。

---

## 还是不行？

如果清除缓存后仍然显示"未知状态"，请检查：

### 1. 确认服务已重启

```bash
cd D:/gk_admin
php start.php restart
```

### 2. 检查翻译文件是否正确加载

打开浏览器控制台，输入：
```javascript
console.log(window.__VUE_APP_TRANS__)
```

查看输出中是否包含 `pendingDraw` 和 `drawing` 键。

### 3. 检查 Vue 组件版本

确认浏览器加载的是最新版本的 `lottery_ticket_activities.vue`：

- Network 标签中找到 `.vue` 或 `.js` 文件
- 查看文件大小和修改时间
- 确认没有 304 缓存响应

---

## 技术细节

**代码位置：**
- 控制器翻译：`ChannelLotteryTicketActivityController.php` Line 79-83
- 翻译文件：`lang/zh-TW/lottery_ticket.php` Line 99-100
- Vue 组件：`views/lottery_ticket_activities.vue` Line 1726-1736

**状态常量映射：**
```php
STATUS_PENDING_DRAW = 5  → 'pendingDraw' → '待開獎'
STATUS_DRAWING = 6       → 'drawing'     → '開獎中'
```

---

**最后更新：** 2026-06-18  
**版本：** v2.0 - 待开奖状态完整实现
